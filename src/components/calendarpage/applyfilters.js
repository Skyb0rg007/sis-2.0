/* Apply Filters
 * A button to apply the filters input in the sidebar
 * Camille Calabrese
 * Polyhack 2019
 */

import React from 'react';
import { Button } from 'react-bootstrap';

const ApplyFilters = (props) => {
    return (
        <Button> Apply Filters </Button>
    )
}

export default ApplyFilters;